image: Visual Studio 2022

configuration:
  - Release
  - Debug

platform:
  - x64

install:
  - git submodule update --init --recursive

build_script:
  - mkdir build
  - cd build
  - cmake -G "Visual Studio 17 2022" -A x64  ..
  - cmake --build .
